// Remembers things until the tab is reloaded
export const session = {
  introPlayed: false,
  entryPath: null as string | null,
  homeScroll: null as number | null,
  selectedService: null as string | null,
};